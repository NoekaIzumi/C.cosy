<%= stylesheet_link_tag 'admin' %>

<%= render partial: "layouts/admin_header" %>

<main>
  <div class = "content">
    <div class = "title">
     <%= User.find(params[:id]).name %>の投稿一覧
    </div></br>
    <%= render partial: "layouts/admin/post", collection: @posts %>
  </div>
</main>
</div>

